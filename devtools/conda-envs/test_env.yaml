name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - gfortran
  - numpy
  - pandas
  - scipy
  - sympy
  - spglib
  - matplotlib
  - tqdm
    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pip:
    - codecov
